# Supabase connection strings — paste yours in once the project is created.
# Transaction pooler (port 6543) — used by the app at runtime
DATABASE_URL=""
# Direct connection (port 5432) — used by Prisma migrations
DIRECT_URL=""

# Auth.js
AUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Single admin login (Phase 1 — seeded as the one User row)
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
